[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations
sth.patches.clear_blank_spb_trans_no
sth.patches.merge_duplicate_spb_trans_no

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated
# perbaiki_restan_data_tbs dan isi_adjustment_sounding sengaja tidak didaftarkan:
# keduanya perbaikan data sekali jalan yang mau diawasi sendiri waktu dijalankan,
# yang restan Data TBS bahkan memposting ulang puluhan Stock Entry. Jalankan
# dengan `bench --site <site> execute sth.patches.<nama>.execute`.
